---
- hosts: 'gw01.casalani.de'
  gather_facts: true
  connection: network_cli
  any_errors_fatal: yes
  tasks:
    - import_role:
        name: vyos

    - name: Save configuration
      vyos_config:
        save: true
