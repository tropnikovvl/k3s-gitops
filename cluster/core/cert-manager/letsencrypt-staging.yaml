---
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt-staging
spec:
  acme:
    email: christian.birkenbeul@tektoria.de
    server: https://acme-staging-v02.api.letsencrypt.org/directory
    privateKeySecretRef:
      name: letsencrypt-staging
    solvers:
      - dns01:
          cloudflare:
            email: christian.birkenbeul@tektoria.de
            apiTokenSecretRef:
              name: cloudflare-api-key
              key: api-key
