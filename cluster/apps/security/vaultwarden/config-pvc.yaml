---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vaultwarden-config-v1
  namespace: security
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
  storageClassName: rook-cephfs
