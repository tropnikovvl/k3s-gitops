---
apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: dashboard
  namespace: network
spec:
  endpoints:
    - dnsName: "traefik-k3s.${SECRET_DOMAIN}"
      recordType: A
      targets:
        - "${LB_INGRESS}"
